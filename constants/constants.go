package constants

const (
	// DefaultBaseURL stores the base API endpoint to use
	DefaultBaseURL = "https://cat-fact.herokuapp.com/"

	// ViperBaseURLKey stores the name of key in Viper where the base API endpoint is stored
	ViperBaseURLKey = "base_url"
)
